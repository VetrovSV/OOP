#ifndef ARRAY_H
#define ARRAY_H

#include <string>
#include <vector>

void array2file(int* a, unsigned n, std::string filename);

void vector2file(std::vector<int> vec, std::string filename);

#endif // ARRAY_H
